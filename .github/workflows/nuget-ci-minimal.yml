on:
  workflow_call:
    inputs:
      release_type:
        required: false
        type: string
        default: ''

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - run: echo "build"

  publish:
    runs-on: ubuntu-latest
    needs: build
    if: inputs.release_type != ''
    permissions:
      contents: write
      packages: write
    steps:
      - run: echo "publish"
