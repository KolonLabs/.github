on:
  workflow_call:
    inputs:
      release_type:
        required: false
        type: string
        default: ''

jobs:
  myjob:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "done"
